import { AppShell } from '@/components/app-shell';
import { PackagingPipeline } from '@/components/packaging-pipeline';

export default function PackagingPage() {
  return (
    <AppShell>
      <PackagingPipeline />
    </AppShell>
  );
}
